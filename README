#install local
npm install -d

#start daemon
forever start ipGeoServer.js --host x.x.x.x --port xxxx
# or short
forever start ipGeoServer.js -h x.x.x.x -p xxxx
# or default 127.0.0.1:5127
forever start ipGeoServer.js